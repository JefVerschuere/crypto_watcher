const initialState = {};

const watcherReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return "reducer test !"
    }
}

export default watcherReducer;